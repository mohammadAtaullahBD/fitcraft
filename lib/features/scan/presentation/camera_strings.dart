/// User-facing strings for the camera capture flow.
class CameraStrings {
  CameraStrings._();

  static const String noCamera = 'No camera found on this device.';
  static const String frontInstruction =
      'Face forward.\nEnsure full body is in frame.';
  static const String sideInstruction =
      'Turn to face left or right.\nEnsure full body is in frame.';
}
